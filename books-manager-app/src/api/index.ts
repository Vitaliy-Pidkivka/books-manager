import { getBooks, addBook, updateBook, deleteBook } from "./books"

export default {
  // books api
  getBooks,
  addBook,
  updateBook,
  deleteBook,
  // other api's will be here, separate it with a comment so that it will be easy to read
}
